# Claude Code Instructions

## Git Rules

- Do NOT run `git add`, `git commit`, `git push`, or any staging/committing commands.
- The only permitted git command is `git init`.
- All git operations (staging, committing, branching, pushing) are handled manually by the user.
